// Package module discovers and loads template modules, parses their manifests,
// resolves dependencies into a stable order, and overlays builtin modules with
// external ones through a Composite loader.
package module
