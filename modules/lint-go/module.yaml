name: lint-go
description: golangci-lint v2 config + lint workflow
version: 2.1.0
language: go
requires: [base-layout]
files:
  - src: ".golangci.yml.tmpl"
    dest: "."
  - src: ".github/workflows/lint.yml"
    dest: "."
emits:
  checks: [lint]
  actions: [arduino/setup-task]
