name: repo-settings-go
description: Declarative remote repo settings (security, rulesets, Actions policy, merge policy)
version: 1.0.0
language: go
requires: [base-layout]
files:
  - src: ".github/keel-settings.yml.tmpl"
    dest: "."
