name: go-mod
description: Go module and cmd/<name> entrypoint
version: 2.0.0
language: go
requires: [base-layout]
files: # dest is always "." — src carries the subdir
  - src: "go.mod.tmpl"
    dest: "."
  - src: "cmd/{{.repo_name}}/main.go.tmpl"
    dest: "."
